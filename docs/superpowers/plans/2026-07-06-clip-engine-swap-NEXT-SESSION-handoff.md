# Clip-Engine Swap — NEXT-SESSION Handoff (Phase 3 + Phase 4)

**Written 2026-07-06.** Phases 1–2 are DONE and merged. This document is what the next session executes. Read it plus `.superpowers/sdd/progress.md` (the SDD ledger) to resume — you should not need to re-derive anything.

---

## 0. Where things stand

- **Branch:** `codex/high-quality-video-retrieval` @ `640f333` (Phase 1+2 fast-forward-merged here; feature branch `clip-engine-swap` deleted; **not pushed** to origin).
- **What's done:** `backend/app/clip_engine/` (Supadata search + expansion + rank; vendored gemini clipper; `run.py` runner; `bridge.py` translator + relevance filter; `metadata.py`). All four `/api/ingest/*` methods (`ingest_url/topic_cut/search/feed`) rewired to the engine, reusing `pipeline._persist_ingest()` so the `ReelOut` contract is byte-identical. iOS + web clients unchanged. **64 clip-engine tests green**; 12 old-path tests `@unittest.skip`'d.
- **Uncommitted WIP already in the tree (NOT ours):** `backend/app/services/reels.py` (+228), `segmenter.py` (+7), `clip_whisper_refine.py`, `llm_router.py` (−3, deletes the API-blackout comment → re-enables Groq Whisper), `backend/tests/test_reels_saliency.py` (+176), plus untracked `audit_*.csv` (user's reference artefacts — leave them). This is a pre-existing "saliency scoring on the legacy path" change. **See Phase 3 Task T0.**

## 1. Resume checklist (conventions)

- **Repo root:** `/Users/vincentfeng/Documents/reelai app/reelai/reelAI copy 2`. Run everything from here.
- **Tests:** `backend/.venv/bin/python -m pytest backend/tests -q` (only this venv has pytest + deps). Single file: `… -m pytest backend/tests/test_clip_engine_ingest_url.py -q`.
- **Imports:** `from backend.app.X import …` (never `from app…` — double-import breaks pydantic). Inside `clip_engine`, relative imports.
- **DB-integrated test harness template:** `backend/tests/test_clip_engine_ingest_url.py` — `TemporaryDirectory` + `os.environ["DATA_DIR"]`, `db_module._db_ready=False`, `get_settings.cache_clear()`, `main_module.settings=get_settings()`, `os.environ["REELAI_INGEST_SKIP_IMPORT_SWEEP"]="1"`, bump `main_module.ingestion_pipeline._rate_limiter = _PlatformRateLimiter(overrides={"yt": (1000, 60.0)})`, and mock `clip_engine_run.clip` + `clip_engine_meta.youtube_metadata` + `clip_engine_search.discover`. Real keys not needed (engine is mocked).
- **clip_engine config = env vars** (from gitignored `backend/.env`): `SUPADATA_API_KEY`, `GEMINI_API_KEY` (+`GOOGLE_API_KEY` fallback), `GROQ_API_KEY`, `GEMINI_MODEL`; knobs `CLIP_ENGINE`, `TRANSCRIBER`, `CLIP_SEARCH_MAX_VIDEOS`, `CLIP_SEARCH_BREADTH`. Phase 3 adds a new flag `REELS_CLIP_ENGINE` (default off).
- **Process:** continue subagent-driven-development (implement→review per task). Ledger: `.superpowers/sdd/progress.md`. Plans: `docs/superpowers/plans/`. Graph: `graphify-out/` (rebuild code graph after edits per project CLAUDE.md).

## 2. RESOLVED DECISIONS (user, 2026-07-06) — these shape Phase 3

1. **No Groq. Free Whisper.** Drop Groq entirely (paid). Any Whisper transcription uses the **free local `faster-whisper`** path. Supadata stays the default *transcript* source for the clip engine (accepted earlier; it's a caption API, not Whisper) — `faster-whisper` is the local fallback. **Revert the uncommitted WIP's Groq-Whisper re-enable in `llm_router.py`** (or let hard-replace delete that path), and in Phase 4 delete `clipper/groq_client.py` + drop `GROQ_API_KEY`. Do NOT set `TRANSCRIBER=groq` / `LLM_PROVIDER=groq` / `JUDGE_PROVIDER=groq` anywhere.
2. **Multiple clips per video (AI decides).** The material path does NOT collapse to one clip/video. Keep ALL of `run.clip`'s topic clips per video (relevance-filtered against the concept via `bridge.filter_by_query`), accumulating toward `num_reels`. So the material helper is **multi-clip** (unlike `ingest_search`, which keeps one-best via `pick_best_clip`).
3. **Concept relevance = practice-folder code.** Do NOT build a separate concept-embedding post-filter. Rely on the practice engine's own topic relevance (pass the concept as the `topic`) plus the existing `bridge.filter_by_query` token-overlap. Use the engine as-is.
4. **WIP disposition = do what's best →** commit the uncommitted `reels.py`/`segmenter.py`/`test_reels_saliency.py` saliency WIP as its OWN standalone commit FIRST (preserve it in history / recoverable), since the hard-replace below supersedes the legacy functions it edits. (Revert only the `llm_router.py` Groq re-enable per decision #1.)
5. **Hard-replace (NO feature flag).** Replace `generate_reels`' internals with the clip-engine path directly — no `REELS_CLIP_ENGINE` flag, no legacy branch kept. This simplifies Phase 3 AND lets Phase 4 delete the legacy modules (`topic_cut.py`, `clip_boundary.py`, `importance_ranker.py`, etc.) right after Phase 3 verifies. Trade-off accepted: no in-code rollback (git revert is the rollback).

> One caveat to raise but not block on: hard-replacing the material path with Supadata-search + Gemini-per-video makes `/api/reels/generate` (synchronous) potentially slow/costly over many concepts × videos. Add a per-generation video cap + concurrency (not a flag) and keep an eye on latency; note it during T4/T5.

## 3. Phase 3 — rewire the study-material path (flag-gated)

**Seam:** `ReelService.generate_reels(material_id, concept_id, …, generation_id, on_reel_created)` at `backend/app/services/reels.py:1487` — the one method both `/api/reels/generate[-stream]` and `/api/feed` refinement converge on. Strategy (**hard-replace, per decision #5**): replace the search+clip internals of `generate_reels` with the clip-engine path — route each extracted concept as a topic through `clip_engine.search.discover` → `clip_engine.run.clip` → `pipeline._persist_ingest`, keeping **multiple clips per video** (decision #2). Delete the legacy `topic_cut`/segment-selection body (no flag, no fallback branch). Preserve everything AROUND search+clip: reels-table row shape + `ReelOut` contract (via `_persist_ingest`), `ranked_feed` scoring, `generation_id`/`reel_generation_heads` tracking, refinement jobs, `reel_feedback`/`record_feedback`, the `on_reel_created` streaming callback, `dry_run` for can-generate, `FeedResponse` pagination. **Load-bearing gap:** `_persist_ingest → upsert_reel_row` hardcodes `generation_id=None`, but feed/refinement key on `generation_id` — thread it through (T1).

**Tasks (ordered, TDD; mirror the harness in §1):**

- **T0 (pre-req):** First **revert the `llm_router.py` Groq-Whisper re-enable** (decision #1 — no Groq). Then commit the remaining uncommitted saliency WIP (`reels.py`/`segmenter.py`/`clip_whisper_refine.py`/`test_reels_saliency.py`) as its OWN standalone commit so it's preserved in history (the hard-replace supersedes the legacy functions it edits — decision #4). Run `pytest backend/tests/test_reels_saliency.py -q` to confirm green before committing.
- **T1:** Thread `generation_id` through the ingest persistence layer. Add `generation_id: str|None=None` to `upsert_reel_row` (`persistence.py:192`; replace hardcoded `row["generation_id"]=None` at ~212) and to `load_existing_reel` (`:152`; change WHERE `generation_id IS NULL OR = ""` at ~176 to `COALESCE(generation_id,'')=COALESCE(?,'')`). Add the param to `_persist_ingest` (`pipeline.py:974`) and pass through. **Test** `test_clip_engine_generation_id.py`: (a) reel persisted with `generation_id="gen-x"` has it in the row + `load_existing_reel` scoped to gen-x finds it; (b) `None` still round-trips (backward-compat; existing idempotency test still passes). Unique index is `(material_id, COALESCE(generation_id,''), video_id, t_start, t_end)` at `db.py:730` — same clip across two generations = two rows, by design.
- **T2:** Add a reusable **multi-clip** `ingest_topic(*, topic, material_id, concept_id, generation_id, exclude_video_ids, target_clip_duration_*, language, max_videos, max_reels=None, on_reel_created=None, dry_run=False)` in `pipeline.py`, sharing the discover→clip→bridge→`_persist_ingest` scaffolding with `ingest_search` (`pipeline.py:807-865`). **Difference from `ingest_search` (decision #2):** per video, do NOT `pick_best_clip` — run `bridge.filter_by_query(engine_out["clips"], transcript, topic)` and persist ALL surviving clips (each a separate reel), stamping `generation_id` and firing `on_reel_created` per reel, stopping once `max_reels` total is reached. Returns `(reels, resolved_video_ids)`. Keep `ingest_search`'s existing one-best-per-video behavior unchanged (extract only the shared discover/clip/persist helpers so both read cleanly). **Test** `test_clip_engine_material_topic.py`: assert multiple reels persist from one video's multi-clip output, all under the given `material_id/concept_id/generation_id`, `exclude_video_ids` honored, `max_reels` cap respected.
- **T3:** Wire `IngestionPipeline` into `ReelService` (hard-replace → pass `ingestion_pipeline` in at construction in `main.py:~272-293`; `ReelService` uses it as the sole search+clip path) + add `_concept_topic_query(concept_row)` (mirror `_build_literal_query` `reels.py:5348`; title + one keyword when title is single-token). **Test** `test_reels_concept_topic.py` (pure, no DB/network).
- **T4:** **Hard-replace** `generate_reels`' search+clip internals (decision #5): load the material's concepts (same query as `reels.py:1550`, scoped by `concept_id`); for each, call `self.ingestion_pipeline.ingest_topic(topic=_concept_topic_query(c), material_id=material_id, concept_id=c.id, generation_id=generation_id, exclude_video_ids=accumulated, max_reels=<remaining budget>, on_reel_created=on_reel_created)`, accumulating exclusions across concepts, stopping at `num_reels` total; convert persisted `ReelOutWithAttribution` → the `ReelOut` dict shape the method returns; honor `dry_run` (T6). **Delete the legacy `topic_cut` branch** (no flag, no fallback). Add a per-generation video cap + light concurrency to bound latency/cost (see §2 caveat). **Test** `test_clip_engine_generate_reels.py` (reels land under the active `generation_id`, `ranked_feed` returns them, multiple clips per video are kept, `on_reel_created` fires per reel, `num_reels` cap respected). Cross-check the pre-existing `test_reels_*` suite: some legacy-path tests will now fail — retire/replace them like the Phase-2 old-path tests (list them for the user).
- **T5:** Verify feed + refinement + feedback end-to-end with the flag on (extend T4's test): `ranked_feed` ranks/paginates the clip-engine reels; refinement with a fresh `result_generation_id` persists + `_activate_generation` swaps `active_generation_id`; `record_feedback` shifts the ranked score. Bump `RANKED_FEED_CACHE_VERSION` (`db.py`) if stale cache could serve pre-flag rows. Full suite stays ≥64 green with flag on AND off.
- **T6:** `dry_run`/can-generate parity: in `ingest_topic`, `dry_run=True` → `discover` only (no `run.clip`/persist), return a viability count; feed it into `generate_reels`'s dry_run so `ReelsCanGenerateResponse` fields stay populated. **Test:** `dry_run=True` writes zero reel rows yet returns a non-empty probe.

## 4. Phase 4 — cleanup (only after Phase 3 verified where noted)

**SAFE NOW (dead on all live paths, independent of Phase 3):**
- Delete entire files: `backend/app/ingestion/adapters/yt_dlp_adapter.py`, `backend/app/ingestion/transcribe.py`.
- In `pipeline.py`: delete `_persist_topic_reels` (~410), `_ingest_url_with_topic_cut` (~646), `_preflight` (~194), the `_feed_executor` attr (~175). Re-verify `_pick_search_adapter` (~883) unused after T2, then delete.
- Remove now-dead `pipeline.py` imports that only served the deleted methods: `normalize_clip_window`, `pick_segments`, `silencedetect` (never referenced), plus `snippet_for_window`, `probe_duration`, `check_ffmpeg_available`, `map_info_dict_to_metadata`, `YtDlpAdapter`, `TempWorkspace`. Run `pytest backend/tests` after to confirm no import breakage.
- Delete the 12 `@unittest.skip("retired … clip-engine-swap")` tests: 7 in `test_ingestion_url.py` (~363,447,495,536,607,650,672), 5 in `test_ingestion_topic_cut.py` (~171,250,291,335,385). Coverage is in `test_clip_engine_*.py`.

**DELETE AFTER Phase 3 (hard-replace removes the legacy `generate_reels` branch that used them — decision #5):** `services/topic_cut.py`, `clip_boundary.py`, `clip_whisper_refine.py`, `segment_features.py`, `importance_ranker.py`, and the parts of `ingestion/segment.py` (`normalize_clip_window`/`snippet_for_window`) still imported by `reels.py` today. Grep each for remaining importers immediately before deleting.

**Groq removal (decision #1):** delete `backend/app/clip_engine/clipper/groq_client.py`, remove the `groq` dep from `requirements.txt`/`pyproject.toml` (grep first), drop `GROQ_API_KEY` from `backend/.env`, and remove any `TRANSCRIBER=groq`/`LLM_PROVIDER=groq`/`JUDGE_PROVIDER=groq` config paths. Keep the free local `faster-whisper` path.

**Dependencies — trim after Phase 3 (hard-replace) is verified.** `faster-whisper`, `sentence-transformers`, `faiss-cpu`, `torch` stay (clip_engine live / material path). `scikit-learn`, `spacy`, `en_core_web_lg`, `networkx` are used ONLY by the material-path `importance_ranker` — deletable once the legacy branch is gone (grep each for other importers first). `groq` deletable per above.

**Other:** No `backend/work` gitignore needed (old code used `tempfile.mkdtemp`). Leave the untracked `audit_*.csv` alone (user's reference artefacts).

## 5. Key rotation

`clip_engine` reads keys from `os.environ` (from gitignored `backend/.env`, `.gitignore:18`). Keys consumed: `SUPADATA_API_KEY` (search + default transcript) and `GEMINI_API_KEY`/`GOOGLE_API_KEY` (segmentation/judge). **`GROQ_API_KEY` is being removed** (decision #1 — no Groq; free `faster-whisper` is the Whisper path, needs no key). To rotate: edit the value in `backend/.env` (never commit) and **RESTART the backend** — `config.py` captures env into module constants at import (no hot reload). ⚠️ The ORIGINAL keys are still committed in `practice/clips/.env` (Supadata `sd_…`, Gemini `AQ…`) — rotate those at the provider and scrub if that repo is ever pushed. `GEMINI_API_KEY_2` in `backend/.env` is a manual spare (clip_engine only reads `GEMINI_API_KEY`/`GOOGLE_API_KEY`).

## 6. Recommended order next session

Decisions in §2 are settled — start executing.
1. **T0** — revert the `llm_router.py` Groq re-enable, then commit the rest of the saliency WIP as its own commit.
2. **Phase 3 T1–T6** (subagent-driven, per-task review): `generation_id` threading → multi-clip `ingest_topic` → wire into `ReelService` → hard-replace `generate_reels` → feed/refinement/feedback verify → dry_run parity. Retire the legacy `test_reels_*` tests that break (like the Phase-2 old-path tests).
3. **Phase 4 "SAFE NOW"** cleanup — independent; run after T1–T2.
4. **After Phase 3 verified:** Phase 4 legacy deletion (`topic_cut.py` etc.) + Groq removal + dep trim (`scikit-learn`/`spacy`/`networkx`/`groq`).
5. **Real Supadata+Gemini smoke test** (needs live keys) → push / deploy when the user asks.
