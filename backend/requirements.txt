fastapi==0.135.1
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic>=2.12.0
pydantic-settings>=2.10.1
requests==2.32.3
# YouTube serves Brotli-compressed pages when the request advertises
# Accept-Encoding: br (which our stealth headers do, to look like a real
# browser). requests/urllib3 auto-decodes br ONLY if the ``brotli`` (or
# ``brotlicffi``) package is installed; otherwise the raw compressed
# bytes land in ``resp.text`` and every ytInitialData extraction fails
# with "title=''" because <title> only appears in the decoded body.
# Pinning this explicitly so Railway containers always have it.
brotli==1.1.0
youtube-transcript-api==1.2.4
openai==2.26.0
numpy==2.3.3
faiss-cpu==1.13.2
python-docx==1.1.2
pypdf==5.3.1
boto3==1.37.1
python-dateutil==2.9.0.post0
psycopg[binary]==3.2.10
resend==2.10.0
yt-dlp==2026.3.17
faster-whisper==1.1.1
google-genai>=1.0.0
groq>=0.12.0
# Sentence boundary detection for ClipBoundaryEngine (Phase A.2). ~50KB, pure
# Python, no C deps — handles abbreviations (Dr./Mr./e.g./decimals) that the
# regex fallback mis-splits on. If this import ever fails, services/sentences.py
# degrades to the regex splitter automatically.
pysbd==0.3.4
