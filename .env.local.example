# Optional:
# - Local two-process dev (Next + uvicorn): set to http://localhost:8000
# Next.js proxies /api to the backend. Keep browser requests same-origin.
DEV_BACKEND_ORIGIN=http://127.0.0.1:8000
RAILWAY_BACKEND_ORIGIN=https://your-backend.up.railway.app
